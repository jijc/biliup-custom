# biliup-custom 上游同步与长期维护原则

> **这是一份长期有效的项目维护章程。**
>
> 它回答的不是“当前改了哪些代码”，而是：当官方 `biliup/biliup` 持续演进时，`biliup-custom` 应该怎样跟进、怎样判断是否采用官方实现、什么时候删除我们的自定义代码，以及怎样保证已有业务能力不会在升级中丢失。
>
> 当前具体自定义功能、modifier 顺序、实现位置和历史变更，请同时阅读 [`CUSTOM_CHANGES.md`](./CUSTOM_CHANGES.md)。

---

## 1. 项目的长期目标

`biliup-custom` **不是要发展成一个长期与官方分离的独立 fork**。

我们的理想状态是：

```text
官方 biliup
████████████████████████████████████████

我们的差异
██
```

而不是：

```text
官方 biliup
████████████████

我们的魔改
████████████████████████████████
```

因此，本项目的长期方向是：

> **官方优先（Upstream First） + 最小差异（Minimal Delta） + 业务需求不退化（No Business Regression）。**

换句话说：

1. 官方能满足的需求，优先使用官方实现。
2. 官方暂时不能满足的需求，才由 `biliup-custom` 补齐。
3. 官方后来完整实现了某项需求，应主动删除我们的重复实现。
4. 官方只实现了一部分，则只保留最小的差异层，而不是保留两套完整逻辑。
5. 我们珍惜的是**业务规则和回归测试**，不是某一段“自己写过的代码”。

---

## 2. 判断一项自定义功能处于什么状态

所有自定义能力都应该逐步归类为以下三种状态之一。

### 🟢 CUSTOM REQUIRED

官方目前没有满足我们的需求，必须继续保留自定义实现。

例如：

- 官方没有对应能力；
- 官方实现缺少关键安全保护；
- 官方行为会破坏现有生产数据；
- 官方实现无法满足我们明确的业务语义。

此时允许保留 modifier，但应尽可能把补丁限制在最小范围内。

### 🟡 UPSTREAM CANDIDATE

官方已经开始覆盖这项需求，但尚未完全满足我们的生产要求。

这类能力应该重点观察官方后续演进。

处理原则：

- 不急于删除现有实现；
- 不因为“官方有了类似功能”就直接切换；
- 对比业务语义，而不是只比较函数名或 UI；
- 如果官方已经覆盖 80%，应该考虑把自定义代码缩减到剩余 20%，而不是继续维护完整旧实现。

### ⚪ UPSTREAMED

官方已经完整满足我们的业务需求和安全要求。

此时应该：

1. 删除对应自定义实现；
2. 删除不再需要的 modifier；
3. 保留或迁移我们的回归测试，用测试继续约束官方行为；
4. 更新 `CUSTOM_CHANGES.md`，标记该能力已由官方接管；
5. 检查其它 modifier 是否还依赖被删除的 helper / marker；
6. 完整跑一次生产门禁。

**不要因为代码是我们以前写的，就继续维护一套已经没有必要存在的实现。**

---

## 3. 采用官方实现之前，必须满足的五个条件

官方代码“能编译”不等于“可以替换我们的实现”。

要淘汰一项自定义实现，至少检查以下五层。

### 3.1 功能等价

官方是否真的覆盖了当前业务需求？

不能只看“有这个功能”，而要比较实际语义。

例如 MP4 转换不能只检查“官方也支持 MP4”，还要继续确认：

- 是否只处理已完成的分段；
- 是否不会碰正在录制的 `.part`；
- 转换失败是否保留源文件；
- 是否先写临时文件；
- 最终文件是否经过有效性确认；
- 源文件在什么时机删除。

### 3.2 数据兼容

升级不能要求用户删除数据库或录像目录来“解决问题”。

重点检查：

- `/opt` 中已有数据库和配置是否兼容；
- `/recordings` 目录结构是否兼容；
- 已有主播配置是否还能读取；
- 上传模板 ID / 关联关系是否保持；
- Cookie / 登录信息是否保持；
- 老文件是否仍能在录播浏览器中找到。

### 3.3 安全语义等价

重点检查“失败时会发生什么”。

例如：

```text
上传分P成功
    ↓
最终投稿失败
    ↓
必须保留本地文件
```

不能因为官方的 happy path 正常，就忽略错误分支。

### 3.4 运行路径等价

必须确认生产环境实际走的是哪条代码路径。

尤其要警惕：

- 新 downloader；
- 新 uploader；
- 新 async actor；
- 新 sync/stream pipeline；
- 新的 postprocessor 调用位置。

两个功能名字相近，不代表运行路径相同。

### 3.5 回归测试覆盖

即使删除我们的实现，也应该尽量保留我们的业务测试。

原则：

> **实现可以变，业务不变量不能消失。**

---

## 4. 每次同步官方代码的标准流程

每次官方 master 有值得跟进的变化时，按以下顺序执行。

### Step 1：锁定比较范围

记录：

```text
上一次生产验证成功的 upstream SHA
当前准备采用的 upstream SHA
```

不要用“好像更新了几个版本”作为审核依据。

### Step 2：完整比较官方差异

获取两个 SHA 之间：

- commit 列表；
- changed files；
- 重要 diff；
- 涉及录制 / 上传 / 下载 / 配置 / WebUI / 数据模型的变化。

### Step 3：建立交叉矩阵

把官方 changed files 与我们的 modifier 目标文件进行交叉。

至少分成：

```text
A. 官方没碰，我们有自定义
B. 官方碰了，我们没自定义
C. 官方和我们都碰了 —— 高风险，必须函数级审核
D. 官方新增了一条替代运行路径 —— 高风险，必须业务链审核
```

### Step 4：函数级语义审核

对于 C / D 类，必须继续检查：

- 官方旧版函数；
- 官方新版函数；
- modifier 输入；
- modifier 最终输出；
- 调用者；
- 错误分支；
- 文件删除和 postprocessor 的调用顺序。

不能只确认 modifier 还能找到 anchor。

### Step 5：寻找“可以退休的自定义代码”

每次同步官方时必须主动问：

> **这次官方更新之后，有没有哪一块我们的代码已经可以删除？**

这是同步流程的一部分，而不是偶尔做一次的大扫除。

### Step 6：执行测试门禁

最低要求：

```text
Python modifier/regression tests
        ↓
当前官方 upstream + 全部 modifier
        ↓
Rust 编译 / 聚焦测试
        ↓
官方 Dockerfile + WebUI 完整构建
        ↓
amd64 构建
        ↓
arm64 构建
        ↓
multi-arch manifest
        ↓
published image pull + smoke
```

任何一步失败，都不能把新 `latest` 当作生产可用版本。

### Step 7：真实生产 canary

CI smoke 只能证明镜像基本可运行，不能证明 B站、NAS、Cookie、网络与真实直播链路全部正常。

大版本或高风险上游升级后，优先用一个主播/一段较短录制做 canary，观察完整链：

```text
发现开播
→ 开始录制
→ 分段完成
→ FLV → MP4
→ 每日编号
→ XML companion
→ 上传各分P
→ build_studio
→ 最终投稿
→ Submit successful
→ 成功后执行 postprocessor/rm
```

确认一条完整链路后，再把它视为“生产实测通过”。

---

## 5. 当前业务不可退化的核心不变量

以下不是“某段实现必须永远存在”，而是**业务结果必须始终成立**。

未来官方如果原生满足这些要求，可以删除我们的实现，但这些要求本身不能静默消失。

### 5.1 录像目录与时间

- 正式录像根目录仍是 `/recordings`；
- 按主播 / 逻辑日期组织；
- `{record_date}` 以 **04:00** 为换日边界；
- 文件名中的 `%Y/%m/%d/%H/%M/%S` 使用真实本地录制时间，不因为逻辑日期而整体偏移。

### 5.2 每日编号

- 同一主播、同一逻辑日按 `01-`、`02-`、`03-`……递增；
- 临时录制文件名不能直接暴露 `{daily_seq}`；
- 视频与对应 XML 使用同一个编号；
- 已编号文件不得重复编号。

### 5.3 FLV → MP4

- 只处理已经完成的 FLV；
- 正在录制的 `.part` 不处理；
- 使用无损 remux（在适用情况下 `-c copy`）；
- 先写临时目标，再原子/安全地生成最终 MP4；
- 转换失败保留原 FLV；
- 只有确认 MP4 成功后才能删除源 FLV。

### 5.4 自动上传和 rm 安全

普通自动上传必须保持：

```text
转换 / 编号
→ 上传分P
→ build_studio
→ 最终 submit
→ 投稿成功
→ postprocessor / rm
```

如果最终投稿失败：

```text
return error
→ 不得执行成功后 postprocessor/rm
```

### 5.5 没有上传模板时保护文件

偶发配置丢失或关联缺失不能成为删除录像的理由。

没有有效上传模板时，应优先保留本地文件，不执行可能具有破坏性的 postprocessor。

### 5.6 手动上传与自动上传相互隔离

手动上传可以拥有额外的超时/反馈保护，但不能为了修手动上传而全局修改自动投稿公共流程。

手动接口“任务已提交”只代表后台任务接受，不代表 B站最终投稿成功。

### 5.7 投稿模板安全

用户填写错误的 strftime 片段不能让后台 async task panic/静默消失。

合法模板应正常格式化；非法模板应安全降级并记录 warning。

### 5.8 通知不能破坏主业务

WxPusher 属于辅助能力。

通知失败不能导致：

- 录制失败；
- 上传失败；
- 投稿失败；
- 文件被错误处理。

### 5.9 配置更新安全

局部修改主播配置时，未提交的字段应保持原值，不能因为前端发送了不完整 entity 而把：

- filename_prefix；
- upload_streamers_id；
- override；
- 其它继承/global 配置

意外覆盖或清空。

### 5.10 可观测性

- 主日志应可从 WebUI/容器侧观察；
- 日志 WebSocket 断开不应拖垮服务；
- 关键阶段应有足够日志判断失败发生在“转换、上传、build studio、最终投稿还是 postprocessor”。

---

## 6. 关于官方新功能：先采用价值，不盲目启用

官方新增功能不代表我们要拒绝；也不代表应该立即在生产中打开。

正确流程是：

```text
官方新增能力
→ 判断是否有价值
→ 对照现有业务不变量
→ 缺失能力列出来
→ 必要时做最小适配
→ 回归测试
→ canary
→ 再生产启用
```

### 当前示例：sync-downloader

官方已经重新实现真正的 `sync-downloader`，属于值得继续关注的能力。

但它是一条独立的边录边传路径，并不等价于当前稳定的：

```text
StreamGears 落盘
→ FLV/MP4
→ 每日编号
→ XML companion
→ 普通自动投稿
```

因此当前状态应视为：

```text
🟡 UPSTREAM CANDIDATE
```

在没有完成我们的业务语义适配和真实 canary 之前：

> **生产环境继续使用当前验证过的 `stream-gears`，不要仅因为官方新增了 Sync 就切换。**

未来如果官方 Sync 已覆盖我们的需求，或者只需要很薄的适配层，就应该考虑迁移，并删除旧的重复实现。

---

## 7. 删除一个 modifier 的正确方式

当某项能力已经由官方完整接管时，不是简单删掉一个 `.py` 文件就结束。

正确步骤：

1. 确认官方实现满足业务不变量；
2. 先让现有回归测试针对官方实现跑通；
3. 删除 modifier；
4. 删除 runtime build chain 中的调用；
5. 删除只服务于该 modifier 的 marker/helper；
6. 检查后续 modifier 是否依赖它；
7. 更新 `CUSTOM_CHANGES.md`；
8. 把状态改为 `⚪ UPSTREAMED`；
9. 完整执行 Rust/Docker/multi-arch/published smoke；
10. 做一次真实生产 canary。

如果测试只能在“我们旧实现存在”时通过，说明还没有真正完成 upstream 接管。

---

## 8. 明确禁止的维护方式

以下做法应避免：

### 禁止 1：为了保持“我们的版本”而拒绝官方更好的实现

代码所有权不是目标，稳定业务才是目标。

### 禁止 2：官方已经实现完整功能，我们仍叠一套相同 modifier

这会制造双重语义和未来冲突。

### 禁止 3：只要能编译就自动跟进官方

编译通过无法证明：

- rm 顺序正确；
- 文件不会丢；
- 配置不会被清空；
- 实际 runtime 没换路径。

### 禁止 4：modifier anchor 还能匹配，就认为官方升级安全

必须检查最终业务语义。

### 禁止 5：为修一个局部问题修改全局公共链路

特别是手动上传与自动上传，应保持作用域隔离。

### 禁止 6：通过删除数据来“解决升级兼容”

数据库、Cookie 和 `/recordings` 属于生产数据，升级镜像不应依赖清库。

### 禁止 7：把聊天记录当唯一知识库

重要决策、业务不变量和维护规则必须落进仓库文档与测试。

---

## 9. 隔几个月重新接手项目时，从这里开始

如果很久没有维护，按以下顺序恢复上下文：

### 第一步

阅读本文件：

```text
UPSTREAM_SYNC_POLICY.md
```

先恢复“我们为什么这样维护”。

### 第二步

阅读：

```text
CUSTOM_CHANGES.md
```

恢复“现在具体有哪些自定义能力、modifier 顺序是什么”。

### 第三步

找到最近一次正式成功发布：

- main commit；
- upstream SHA；
- GHCR image digest；
- amd64 / arm64 / manifest / published smoke 是否全绿。

### 第四步

把“上一次已验证 upstream”与“当前官方 master”做 compare。

不要直接拿当前 master 开始修 modifier。

### 第五步

建立 changed-files × modifier-targets 交叉矩阵。

先审交叉，再写代码。

### 第六步

每次同步都同时寻找：

```text
新冲突？
新能力？
可以退休的自定义实现？
```

---

## 10. 文档职责分工

为了避免以后又只依赖记忆，仓库文档职责固定如下：

### `UPSTREAM_SYNC_POLICY.md`

记录长期原则：

- 为什么要 upstream-first；
- 如何判断官方是否可以接管；
- 如何审核升级；
- 哪些业务不变量不能退化；
- 如何删除旧 modifier。

这份文档应尽量稳定，不记录大量一次性修 Bug 细节。

### `CUSTOM_CHANGES.md`

记录当前事实：

- 现在有哪些 modifier；
- 每个 modifier 做什么；
- 改官方哪些文件；
- 当前运行顺序；
- 历史重要故障与决策；
- 当前生产注意事项。

每次新增、修改或删除自定义能力，都必须同步更新。

### `tests/`

记录真正可执行的业务契约。

最终应尽量做到：

> 聊天记录丢了没关系；只看仓库文档 + 测试，也能继续安全维护项目。

---

## 11. 最终原则

以后维护 `biliup-custom` 时，始终记住以下四句话：

> **跟随官方，但不盲从官方。**
>
> **保留需求，不执着于保留自己的实现。**
>
> **能删掉的自定义代码，比新增一层补丁更有价值。**
>
> **实现可以越来越官方，业务测试和安全底线不能丢。**
